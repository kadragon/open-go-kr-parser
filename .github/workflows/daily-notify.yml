# Trace: spec_id=SPEC-scheduler-001 task_id=TASK-0003
name: Daily Document Notification

on:
  schedule:
    # Run at 00:00 UTC (09:00 KST) Monday to Friday
    - cron: '0 0 * * 1-5'

  workflow_dispatch:
    inputs:
      target_dates:
        description: 'Target dates (YYYY-MM-DD, comma-separated), defaults to auto-calculated'
        required: false
        type: string

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .

      - name: Run notification service
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          if [ -n "${{ inputs.target_dates }}" ]; then
            open-go-kr --dates "${{ inputs.target_dates }}"
          else
            open-go-kr
          fi
